[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--production=false"
  REACT_APP_API_URL = "https://granular-certificate-registry.onrender.com"